{
  "name": "platform-live-acceptance-fixture",
  "private": true,
  "scripts": {
    "build": "cd tests/fixtures/apps/bun && bun run build",
    "start": "cd tests/fixtures/apps/bun && bun run start"
  }
}
